#!/usr/bin/env bash
set -euo pipefail

# Development convenience wrapper; setup itself needs no checkout.
repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
uv tool install --force "${repo_dir}"
exec "$(uv tool dir --bin)/ulanzi-niri" setup "$@"
